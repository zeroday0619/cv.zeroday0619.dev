# CV (LaTeX)

## Build
- Recommended: `latexmk`

```bash
latexmk -pdf cv.tex
```

## Clean
```bash
latexmk -c
```

## Notes
- Edit `cv.tex` and replace placeholders (name, contact, experience bullets).
- Keep bullets impact-focused (action + scope + metric).
